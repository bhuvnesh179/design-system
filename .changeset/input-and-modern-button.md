---
'@100xbansal/ui': minor
---

Add `Input` component with label, hint, error/success states, sizes, `outline`/`filled` variants, icons, addons, clear button, password toggle, character count, and loading state.

Refresh `Button` styling (softer shadows, larger radius, press feedback) and add `outline`, `soft`, and `link` variants, a `pill` shape, `iconOnly`, and `loadingText`.
