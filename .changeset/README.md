# Changesets

Run `pnpm changeset` to describe a change to a published package (`@100xbansal/ui`, `@100xbansal/tokens`). Each changeset records which packages changed, the semver bump, and a changelog entry. `pnpm version-packages` applies them; `pnpm release` builds and publishes.
